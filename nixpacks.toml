# Railway (Nixpacks) 用: devDependencies も含めてインストールしてからビルド
[variables]
NPM_CONFIG_PRODUCTION = "false"

[phases.install]
cmds = ["npm ci"]

[phases.build]
cmds = ["npm run build"]

[start]
cmd = "npm run start"
